#  Упорядочить элементы массива A размером 9 и B разером 11, расположенные после максимального элемента. Упорядочение части массива, начинающейся элементом с заданным индексом, осуществить методом
print('Задание 2.8')
def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and key < arr[j]:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key

def sort_after_max(arr):
    if not arr:
        return
    max_index = arr.index(max(arr))
    if max_index < len(arr) - 1:
        sub_array = arr[max_index + 1:]
        insertion_sort(sub_array)
        arr[max_index + 1:] = sub_array

def get_array_elements(prompt, expected_length):
    while True:
        arr = list(map(int, input(prompt).split()))
        if len(arr) == expected_length:
            return arr
        print(f"Ошибка: Пожалуйста, введите ровно {expected_length} элементов.")

def main():
    
    A = get_array_elements("Введите 9 элементов массива A, разделенных пробелами: ", 9)
    B = get_array_elements("Введите 11 элементов массива B, разделенных пробелами: ", 11)

    print("Исходный массив A:", A)
    print("Исходный массив B:", B)

    sort_after_max(A)
    print("Отсортированный массив A после максимального элемента:", A)
    sort_after_max(B)
    print("Отсортированный массив B после максимального элемента:", B)

if __name__ == "__main__":
    main()
